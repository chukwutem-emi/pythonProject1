# A tuple in Python is similar to a list. The difference between
# the two is that we cannot change the elements of a tuple once
#  it is assigned whereas we can change the elements of a list.
# we use parenthesis instaed of square bracket.

# example:

numbers = (1, 2, 3)
print(numbers[0])